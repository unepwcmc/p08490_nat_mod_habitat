// The trainlines dataset (from OSM) kept causing ‘internal errors’ when trying to upload the 
// shapefile to Google Earth Engine, so the data were prepared outside of Google Earth Engine and uploaded a TIFF file
